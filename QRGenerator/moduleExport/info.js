const name = name => {
    return name;
}
const age = age => {
    return age;
}
const address = address => {
    return address;
}
const hobbies = hobbies => {
    return hobbies;
}

module.exports = {
    name,
    age,
    address,
    hobbies
}